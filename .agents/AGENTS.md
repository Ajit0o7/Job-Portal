# Project Git Rules
- **Authentication Method**: Always configure the remote `origin` using the HTTPS URL (`https://github.com/Ajit0o7/...`). Do not use SSH remote configurations (`git@github.com:...`).
- **Account**: Pushes must be performed using the cached credentials for user `Ajit0o7` (magarishor identity must not be used).
